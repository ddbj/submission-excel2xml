# frozen_string_literal: true

RSpec.describe Submission::Excel2xml do
  it "has a version number" do
    expect(Submission::Excel2xml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
